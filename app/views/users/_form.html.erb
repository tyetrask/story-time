<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <label><%= f.label :email %></label>
    <%= f.text_field :email, class: 'form-control', placeholder: 'Enter Email Address...' %>
  </div>
  
  <div class="form-group">
    <label><%= f.label :password %></label>
    <%= f.text_field :password, class: 'form-control', placeholder: 'Enter Password...' %>
  </div>
  
  <div class="form-group">
    <label><%= f.label :pivotal_api_token %></label>
    <%= f.text_field :pivotal_api_token, class: 'form-control', placeholder: 'Enter Pivotal API Token...' %>
  </div>
  <div class="actions">
    <%= f.submit 'Update', class: 'btn btn-success' %>
  </div>
<% end %>
